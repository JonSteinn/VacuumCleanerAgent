apply plugin: 'java'

repositories {
    jcenter()
}

sourceSets {
    main {
        java {
            srcDir 'src'
        }
    }
    test {
        java {
            srcDirs = ["test"]
        }
    }
}

dependencies {
    testCompile "junit:junit:4.11"
}